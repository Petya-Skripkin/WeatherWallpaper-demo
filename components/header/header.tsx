import { Text } from "react-native";
import { HeaderContainer, LogoText } from "./styles";

export default function Header() {
  return (
    <HeaderContainer>
      <LogoText>Weather Wallpaper</LogoText>
    </HeaderContainer>
  );
}
